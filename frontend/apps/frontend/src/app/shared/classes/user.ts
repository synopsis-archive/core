export class User {
  constructor(
    // name of user
    public username: string,
    // clazz of user - example: 5B
    public clazz: string,
    // role of user
    public role: string,
  ) {
  }
}
